<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Редактировать экспонат | Админ-панель</title>
  <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
  <div class="container">
    <div class="admin-nav">
      <a href="/admin">Назад</a>
      <a href="/admin/logout">Выйти</a>
    </div>
    <h2>Редактировать экспонат</h2>
    <form method="post" action="/admin/edit/<%= exhibit.id %>" enctype="multipart/form-data">
      <input name="name" value="<%= exhibit.name %>" required>
      <input name="year" value="<%= exhibit.year || '' %>">
      <textarea name="description"><%= exhibit.description || '' %></textarea>
      <input type="file" name="image">
      <% if (exhibit.image) { %>
        <br>Текущее изображение: <img src="<%= exhibit.image %>" style="max-width:100px;">
      <% } %>
      <button type="submit">Сохранить</button>
    </form>
  </div>
</body>
</html>